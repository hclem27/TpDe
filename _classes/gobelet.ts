export class Gobelet{
    private _valeur: number;

    constructor(_value: number)
    {
        this._valeur = this._valeur;
    }
    lancer(): number{
        return Math.floor(Math.random())
    }
    afficherScore(_valeur: number){
            
    }
}